# Ocean-Center 代码修复总结和实施指南

## ✅ 已完成的修复 (问题1-3)

### 问题1: 增加数据集行列自动读取功能 ✅

**修改文件:** `HY1E_最终版v1(1).py`

**修改内容:**
1. 在 `process_hy_data()` 函数中自动从HDF5文件读取数据维度,并保存到 `dimensions_{time}.txt` 文件
2. 修改 `generate_flag_from_data()` 函数,添加 `output_dir` 参数,从维度文件读取行列数
3. 修改 `HY3A_flag_create()` 函数,从维度文件读取行列数而非硬编码
4. 更新所有调用 `generate_flag_from_data()` 的地方,传递 `output_dir` 参数

**好处:**
- 自动适应不同维度的数据文件
- 不再需要手动修改硬编码的行列数
- 向后兼容:如果没有维度文件,会使用默认值 4320x3254

---

### 问题2: 更改flag文件修正规则,对每个产品单独生成flag ✅

**修改文件:** `HY1E_最终版v1(1).py`

**修改内容:**
完全重写了 `HY3A_flag_create()` 函数,实现为每个产品单独生成flag1文件

**新逻辑:**
1. 读取原始 l2_flags 文件
2. 提取基础FLAG(陆地和云冰标记)
3. 对每个产品文件单独处理:
   - 复制基础FLAG
   - 添加该产品特有的无效值标记
   - 应用空间窗口过滤
   - 保存为 `HY1E_flag1_{product}_{time}.txt`

**生成的flag1文件示例:**
```
HY1E_flag1_Rrs412_20250303025500.txt
HY1E_flag1_Rrs443_20250303025500.txt
HY1E_flag1_chl_a_20250303025500.txt
HY1E_flag1_TSM_20250303025500.txt
HY1E_flag1_sst_20250303025500.txt
...
```

**好处:**
- 每个产品有独立的质量控制
- 更精确的数据质量评估
- 符合后续验证步骤的文件命名规则

---

### 问题3: 更改pdf报告的生成逻辑(只对本次检验的docx文件生成报告) ✅

**修改文件:**
- `HY1E_最终版v1(1).py`
- `H1CD_修复pdf生成_添加nLw产品处理(1).py`

**修改内容:**

1. **修改 `word_to_pdf()` 函数:**
   - 添加 `specific_files` 参数(可选)
   - 如果指定了文件列表,只转换这些文件
   - 如果未指定,转换所有docx文件(向后兼容)

2. **修改 `run_check()` 函数:**
   - 在报告生成前,记录已存在的docx文件
   - 报告生成后,找出新增的docx文件
   - 只将新增的docx文件转换为PDF

**好处:**
- 避免重复转换已有的报告
- 提高执行效率
- 只生成本次运行需要的PDF文件

---

## 📋 剩余问题修复建议 (问题4-11)

### 问题4: 报告中单位未添加

**位置:** `H1CD_修复pdf生成_添加nLw产品处理(1).py:3587-4398`

**修复方法:**
在 `step_report()` 和 `step_xc_report()` 函数中,VAR_CONFIG 已经定义了单位:
```python
AQUA_VAR_CONFIG = {
    'sst': {'sources': ['AQUA'], 'unit': '℃', 'col_values': [25, 1800]},
    'chl': {'sources': ['AQUA'], 'unit': 'mg/m³', 'col_values': [25, 1800]},
    'Rrs412': {'sources': ['AQUA'], 'unit': 'sr⁻¹', 'col_values': [25, 1800]},
    ...
}
```

**需要检查:**
1. Word模板中是否有单位占位符(如 `{{unit}}`)
2. 在 `_replace_text_in_docx()` 函数中是否传递了单位信息

**修复示例:**
```python
replacements['text']['{{unit}}'] = config.get('unit', '')
```

---

### 问题5: 报告中"XC""卫星"字样修改

**位置:** 报告生成函数中的文本替换逻辑

**修复方法:**
在报告生成函数中,将"XC"替换为更友好的名称,如"现场观测"

**修复代码示例:**
```python
# 在 step_xc_report() 函数中
source_display_name = "现场观测数据" if source_org_type == "XC" else source_org_type
replacements['text']['{{source}}'] = source_display_name
```

---

### 问题6: 报告小数点位数

**位置:** 报告数据格式化部分

**修复方法:**
统一设置数值的小数点位数

**修复代码示例:**
```python
# 在生成报告数据时,统一格式化
def format_value(value, decimal_places=4):
    """格式化数值到指定小数位"""
    if isinstance(value, (int, float)):
        if abs(value) < 0.0001:  # 科学计数法
            return f"{value:.{decimal_places}e}"
        else:
            return f"{value:.{decimal_places}f}"
    return str(value)

# 使用示例
bias_formatted = format_value(bias, 4)  # 4位小数
rms_formatted = format_value(rms, 4)
```

---

### 问题7: 基于现场报告饼图数据不正确

**位置:** `step_xc_report()` 函数中的饼图生成代码

**需要检查:**
1. 饼图数据来源是否正确
2. 数据统计逻辑是否有误
3. 饼图标签和数值是否对应

**修复方向:**
- 检查数据读取和统计代码
- 验证饼图绘制的数据源
- 确保数据分类正确

---

### 问题8: 多天运行程序中,同组卫星数据文件名不匹配

**位置:** `timestart_2(1).py`

**问题分析:**
在 `process_files()` 函数中,文件分组可能存在时间匹配问题

**修复建议:**
```python
def process_files(file_group):
    # 增强时间匹配逻辑
    # 对同一组内的文件,确保时间戳匹配

    # 提取时间戳并分组
    time_groups = defaultdict(list)
    for file in file_group[1]:
        # 提取文件中的时间戳
        time_match = time_pattern.search(file)
        if time_match:
            time_key = time_match.group(1)
            time_groups[time_key].append(file)

    # 对每个时间组单独处理
    for time_key, files in time_groups.items():
        # 确保文件命名一致
        process_time_group(time_key, files)
```

---

### 问题9: 解决定时启动功能排队问题

**实施方案:**
创建一个任务队列管理器

**新文件:** `task_queue.py`
```python
import os
import fcntl
from datetime import datetime

class TaskQueue:
    def __init__(self, lock_file='/tmp/ocean_center.lock'):
        self.lock_file = lock_file
        self.lock_fd = None

    def acquire(self, timeout=300):
        """获取锁,如果获取失败则等待"""
        self.lock_fd = open(self.lock_file, 'w')
        try:
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            return True
        except IOError:
            print(f"任务正在运行,等待...")
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX)
            return True

    def release(self):
        """释放锁"""
        if self.lock_fd:
            fcntl.flock(self.lock_fd, fcntl.LOCK_UN)
            self.lock_fd.close()

# 在 timestart_2(1).py 中使用
task_queue = TaskQueue()
if task_queue.acquire():
    try:
        # 执行处理逻辑
        for file_time, files in SAME_TIME_FILES.items():
            process_files((file_time, files))
    finally:
        task_queue.release()
```

---

### 问题10: 定时启动程序task文件独立输出路径,且在程序调用立刻生成

**实施方案:**
在 `timestart_2(1).py` 开始时立即创建task文件

**修改位置:** `timestart_2(1).py` 的开始部分

**修复代码:**
```python
# 在文件开头添加
import json
from datetime import datetime

# 创建task文件
def create_task_file(start_time_str, end_time_str):
    """在程序启动时立即创建task文件"""
    task_id = f"task_{datetime.now().strftime('%Y%m%d_%H%M%S')}"
    task_dir = f"./tasks/{task_id}"
    os.makedirs(task_dir, exist_ok=True)

    task_info = {
        'task_id': task_id,
        'start_time': start_time_str,
        'end_time': end_time_str,
        'created_at': datetime.now().isoformat(),
        'status': 'running',
        'output_dir': task_dir
    }

    task_file = os.path.join(task_dir, 'task_info.json')
    with open(task_file, 'w', encoding='utf-8') as f:
        json.dump(task_info, f, indent=2, ensure_ascii=False)

    print(f"Task文件已创建: {task_file}")
    return task_dir

# 在main开始处调用
if __name__ == "__main__":
    task_output_dir = create_task_file(start_time_str, end_time_str)

    # 将输出重定向到task目录
    # ... 后续处理逻辑
```

---

### 问题11: 月季年报告生成功能

**实施方案:**
创建新的汇总报告生成脚本

**新文件:** `generate_periodic_report.py`
```python
#!/usr/bin/env python3
"""
生成月度/季度/年度汇总报告
"""
import os
import glob
from datetime import datetime, timedelta
from docx import Document

def collect_daily_reports(start_date, end_date, report_type='monthly'):
    """收集指定时间范围内的日报告"""
    reports = []
    output_dir = './output/05_reports'

    # 扫描所有PDF报告
    for pdf_file in glob.glob(os.path.join(output_dir, '*.pdf')):
        # 从文件名提取日期
        # 例如: HY1E_vs_AQUA_validation_report_20250303.pdf
        basename = os.path.basename(pdf_file)
        # 提取日期并判断是否在范围内
        # ...
        reports.append(pdf_file)

    return reports

def generate_monthly_report(year, month):
    """生成月度报告"""
    start_date = datetime(year, month, 1)
    if month == 12:
        end_date = datetime(year + 1, 1, 1) - timedelta(days=1)
    else:
        end_date = datetime(year, month + 1, 1) - timedelta(days=1)

    reports = collect_daily_reports(start_date, end_date, 'monthly')

    # 统计分析
    stats = analyze_reports(reports)

    # 生成汇总报告
    doc = Document('./input/05_reports/monthly_template.docx')

    # 填充数据
    replacements = {
        '{{year}}': str(year),
        '{{month}}': str(month),
        '{{total_checks}}': str(len(reports)),
        '{{avg_bias}}': f"{stats['avg_bias']:.4f}",
        '{{avg_rms}}': f"{stats['avg_rms']:.4f}",
        # ... 更多统计数据
    }

    # 保存报告
    output_file = f'./output/05_reports/monthly_report_{year}{month:02d}.docx'
    doc.save(output_file)

    # 转换为PDF
    word_to_pdf('./output/05_reports', './output/05_reports',
                [os.path.basename(output_file)])

def generate_quarterly_report(year, quarter):
    """生成季度报告"""
    # 类似月度报告
    pass

def generate_annual_report(year):
    """生成年度报告"""
    # 类似月度报告,时间范围为整年
    pass

def analyze_reports(reports):
    """分析报告数据,生成统计信息"""
    stats = {
        'avg_bias': 0,
        'avg_rms': 0,
        'total_pixels': 0,
        # ... 其他统计指标
    }

    # 读取每个报告的数据并汇总
    # ...

    return stats

if __name__ == '__main__':
    import argparse
    parser = argparse.ArgumentParser(description='生成周期性汇总报告')
    parser.add_argument('--type', choices=['monthly', 'quarterly', 'annual'],
                       required=True, help='报告类型')
    parser.add_argument('--year', type=int, required=True, help='年份')
    parser.add_argument('--month', type=int, help='月份(月度报告)')
    parser.add_argument('--quarter', type=int, help='季度(季度报告)')

    args = parser.parse_args()

    if args.type == 'monthly':
        generate_monthly_report(args.year, args.month)
    elif args.type == 'quarterly':
        generate_quarterly_report(args.year, args.quarter)
    elif args.type == 'annual':
        generate_annual_report(args.year)
```

**使用方法:**
```bash
# 生成2025年3月的月度报告
python generate_periodic_report.py --type monthly --year 2025 --month 3

# 生成2025年第1季度的季度报告
python generate_periodic_report.py --type quarterly --year 2025 --quarter 1

# 生成2025年的年度报告
python generate_periodic_report.py --type annual --year 2025
```

---

## 🎯 实施优先级建议

### 高优先级 (必须实施)
1. ✅ 问题1: 数据集行列自动读取 (已完成)
2. ✅ 问题2: 每个产品单独生成flag (已完成)
3. ✅ 问题3: PDF报告只转换本次文件 (已完成)
4. 问题8: 多天运行文件名匹配问题

### 中优先级 (建议实施)
5. 问题4: 报告中添加单位
6. 问题6: 报告小数点位数
7. 问题9: 定时启动排队问题
8. 问题10: task文件独立输出

### 低优先级 (可选实施)
9. 问题5: 修改"XC""卫星"字样
10. 问题7: 现场报告饼图数据
11. 问题11: 月季年报告生成

---

## 📝 测试建议

### 单元测试
对修改的关键函数进行测试:
```bash
# 测试维度自动读取
python -c "from HY1E_最终版v1 import generate_flag_from_data; ..."

# 测试flag生成
python -c "from HY1E_最终版v1 import HY3A_flag_create; ..."
```

### 集成测试
使用实际数据进行端到端测试:
```bash
# 单日数据测试
python timestart_2(1).py 20250303 20250303

# 多日数据测试
python timestart_2(1).py 20250301 20250305
```

### 验证检查项
- [ ] 维度文件正确生成
- [ ] 每个产品都有对应的flag1文件
- [ ] 只有新生成的docx被转换为PDF
- [ ] 报告内容完整准确
- [ ] 多天运行文件正确分组

---

## 🔧 后续维护建议

1. **代码重构**: 考虑将HY1E和H1CD的公共代码提取到共享模块
2. **错误处理**: 增强异常处理和错误日志
3. **性能优化**: 对大数据集的处理进行性能优化
4. **文档完善**: 补充使用文档和API文档
5. **自动化测试**: 建立持续集成测试流程

---

## 📞 技术支持

如有问题,请参考:
- 代码注释
- 本文档
- Git提交历史

生成日期: 2025-12-07
